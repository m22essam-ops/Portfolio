# This file is out of date. Read `READ ME.md` instead.

These notes described the site as it was in the middle of 2026 and most of it
has since been replaced. It talked about a word chain on the home page, a
three-version shuffle, a separate `contact.html`, a grain background, and a
workflow where you downloaded `content.js` by hand and dropped it into the
folder. **None of that is how the site works now.**

Kept only so that anything pointing at this filename still finds something.
The current handbook is **`READ ME.md`**, in this folder.
